# @package _global_

# Ablation: proposed model WITHOUT pair-order consistency loss
# Uses BCE+Dice only, no pair-order regularisation

defaults:
  - override /model: proposed_effnet
  - override /loss: bce_dice
  - _self_
